# Drawry 요구사항 및 개발 계획

> 상태: 1차 MVP 구현 완료, 양 플랫폼 실기기 검증 및 배포 준비 전
>
> 대상 플랫폼: iOS, Android
>
> 구현 언어 및 기술 스택: Flutter 3.44.7·Dart 3.12 단일 코드베이스 확정

## 1. 프로젝트 개요

Drawry는 사진이나 짧은 영상을 꾸며 일기로 기록하고, 기록을 캘린더 또는 SNS형 피드로 다시 볼 수 있는 iOS·Android 그림일기장 애플리케이션이다.

이 문서에서 "SNS형"은 다른 사용자에게 공개하는 소셜 네트워크가 아니라, 본인의 일기를 인스타그램과 유사한 피드 UI로 탐색하는 방식만을 의미한다. 앱 내부의 일기와 프로필을 다른 사용자에게 공개하는 기능은 제공하지 않는다.

## 2. 제품 목표

- 사진과 영상을 이용해 일상의 기록을 쉽고 재미있게 작성할 수 있어야 한다.
- 기록을 날짜 중심의 캘린더와 이미지 중심의 피드 두 방식으로 편리하게 탐색할 수 있어야 한다.
- 이모지, 필터, 텍스트 등의 간단한 편집 기능으로 사용자가 기록을 꾸밀 수 있어야 한다.
- 개인적인 일기와 미디어를 기기 내부에 안전하게 보관할 수 있어야 한다.
- 화면 표시, 공유 항목, 잠금 시간과 날씨 표시처럼 취향에 따라 달라질 수 있는 동작은 가능한 범위에서 사용자 선택 옵션으로 제공하고 마지막 선택을 기억해야 한다.
- 암호화 강도, 공유 파일의 위치 메타데이터 제거, 데이터 무결성과 같이 안전을 위해 필요한 규칙은 사용자 옵션으로 완화하지 않는다.

## 3. 대상 사용자

- 사진이나 영상 위주로 일상을 기록하고 싶은 사용자
- 긴 글보다 시각적인 일기 작성 방식을 선호하는 사용자
- 개인 기록의 보안과 기기 내 보관을 중요하게 생각하는 사용자

## 4. 기능 요구사항

### 4.1 일기 등록

- 사용자는 한 개 이상의 사진 또는 영상을 선택하거나 촬영해야 일기를 등록할 수 있다.
- 일기의 날짜와 시간은 항상 저장하되 현재 시각을 기본값으로 제공하고, 사용자가 입력 필드에서 선택적으로 변경할 수 있어야 한다.
- 사용자는 오늘의 한 줄과 일기 내용을 선택적으로 입력할 수 있어야 한다.
- 저장 전에 최종 결과를 미리 볼 수 있어야 한다.
- 1차 MVP는 균형 프로필을 사용하여 일기당 미디어 최대 10개, 영상 최대 3개·각 30초·총 60초, 전체 가져오기 용량 300MB로 제한한다. 이 값은 실기기 측정 후 조정할 수 있다.

#### 입력 필드 제안

사진 또는 영상만 필수이며, 나머지 입력 필드는 모두 선택 사항으로 한다.

| 필드 | 구분 | 입력 방식 및 목적 |
| --- | --- | --- |
| 사진 또는 영상 | 필수 | 촬영하거나 라이브러리에서 한 개 이상 선택하는 일기의 중심 기록 |
| 날짜 및 시간 | 선택 | 현재 시각을 기본값으로 자동 저장하고 사용자가 변경 가능 |
| 오늘의 한 줄 | 선택 | 피드에서 함께 보여줄 짧은 제목 또는 요약 |
| 일기 내용 | 선택 | 여러 줄 자유 형식 텍스트 |
| 오늘의 기분 | 선택 | 하나의 이모지 또는 감정 아이콘 선택 |
| 태그 | 선택 | 여러 개를 입력하여 추후 검색과 분류에 활용 |
| 날씨 | 선택 | 1차 MVP에서는 사용자가 과거 날씨를 아이콘·기온·강수 여부로 직접 기록하고 변경 가능 |
| 함께한 사람 | 선택 | 이름 또는 별칭을 자유롭게 기록 |

- 필수 필드는 가능한 한 최소화하고, 선택 필드는 작성 화면에서 접거나 펼칠 수 있어야 한다.
- 일기 내용 없이 사진 또는 영상만으로도 저장할 수 있어야 한다.
- 미디어에 촬영 날짜 정보가 있고 사용자가 허용한 경우, 해당 값을 일기 날짜의 선택지로 제안할 수 있어야 한다.
- 여러 미디어의 촬영 날짜가 서로 다르면 후보를 목록으로 보여주고 사용자가 일기의 대표 날짜를 선택할 수 있어야 한다.
- 1차 MVP는 미디어 GPS를 읽지 않고 위치 권한을 요청하지 않으며 장소 필드와 자동 위치 기반 날씨 API를 제공하지 않는다.
- 장소 기록과 자동 과거 날씨는 2차, 네이버 지도는 3차 구현으로 분리한다. 상세 계획은 [장소·네이버 지도 도입 계획](readme/지도-도입-계획.md)을 따른다.

### 4.2 미디어 편집

- 이미지에 이모지 또는 스티커를 배치할 수 있어야 한다.
- 이미지에 필터를 적용할 수 있어야 한다.
- 이미지에 텍스트를 추가하고 위치를 조정할 수 있어야 한다.
- 편집 작업을 취소하거나 다시 적용할 수 있어야 한다.
- 영상 편집은 MVP에서 제외하고 재생, 대표 프레임 선택과 정지 이미지 공유만 지원하며 고급 편집은 추후 검토한다.

### 4.3 일기 탐색

- 사용자는 캘린더에서 날짜별 일기 등록 여부를 확인할 수 있어야 한다.
- 날짜를 선택하면 해당 날짜의 일기 목록 또는 상세 내용을 볼 수 있어야 한다.
- 사용자는 이미지 중심의 SNS형 피드에서 일기를 최신순으로 탐색할 수 있어야 한다.
- 여러 미디어가 있는 일기는 카드 보기에서 가로 슬라이드로 탐색할 수 있어야 한다.
- 사용자는 세로 카드, 2열 격자와 3열 격자 중 피드 레이아웃을 선택할 수 있어야 하며 앱은 마지막 선택을 기억해야 한다.
- 캘린더 보기와 피드 보기 사이를 쉽게 전환할 수 있어야 한다.
- 일기 상세 화면에서 원본 내용, 작성 일시와 미디어를 확인할 수 있어야 한다.

### 4.4 일기 수정 및 삭제

- 사용자는 기존 일기의 텍스트, 날짜와 미디어 편집 내용을 수정할 수 있어야 한다.
- 사용자는 일기를 삭제할 수 있어야 한다.
- 삭제 전에 확인 절차를 제공해야 한다.
- 삭제한 일기는 암호화된 휴지통으로 이동하고 30일 동안 복원할 수 있어야 한다.
- 휴지통 보관 기간이 끝난 일기는 자동으로 영구 삭제하며 사용자가 즉시 영구 삭제할 수도 있어야 한다.
- 모바일 운영체제는 앱이 실행되지 않는 동안 정확한 시각의 백그라운드 작업을 보장하지 않으므로, 30일이 지난 항목은 앱 실행·복귀 시 반드시 정리하고 예약 백그라운드 작업은 보조 수단으로 사용한다.

### 4.5 보안 저장

- 일기 텍스트와 사진·영상은 앱의 내부 저장소에 보관해야 한다.
- 내부에 저장되는 일기 데이터와 미디어는 암호화해야 한다.
- 암호화 키는 일기 데이터와 분리하여 운영체제가 제공하는 안전한 키 저장소에 보관해야 한다.
- 앱 잠금은 사용자가 설정에서 켜거나 끌 수 있어야 하고 생체 인증과 운영체제의 기기 암호를 지원해야 한다.
- 사용자는 앱을 벗어난 뒤 다시 인증할 때까지의 시간을 제공되는 잠금 시간 옵션 중에서 선택할 수 있어야 한다.

### 4.6 설정

- 사용자는 앱 잠금과 개인정보 보호 관련 옵션을 관리할 수 있어야 한다.
- 사용자는 저장 공간 사용량과 앱 정보를 확인할 수 있어야 한다.
- MVP에서 암호화된 백업 파일의 생성과 복원을 지원해야 한다.
- 사용자는 데이터 전체 삭제를 실행할 수 있어야 한다.

### 4.7 사용자 계정 및 동기화

- 계정, 서버 저장 및 여러 기기 간 동기화 기능의 필요 여부는 추후 결정한다.
- 서버 기능이 결정되기 전까지 로컬 우선 저장을 기본 원칙으로 한다.

### 4.8 알림 및 외부 연동

- 사용자는 선택한 일기를 한 장의 이미지로 만들어 기기의 공유 기능을 통해 내보낼 수 있어야 한다.
- 영상이 포함된 일기도 외부로 공유되는 결과물은 정지 이미지여야 한다.
- 공유 이미지를 생성하기 전에 결과를 미리 보고 공유를 취소할 수 있어야 한다.
- 공유를 실행할 때마다 한 줄, 본문, 날짜, 기분과 날씨 중 포함할 항목을 선택할 수 있어야 한다.
- 공유 항목 선택 화면은 직전에 사용한 선택 상태를 기본값으로 복원해야 한다.
- 앱이 사용자의 일기를 자체적으로 게시하거나 다른 사용자에게 공개해서는 안 된다.
- 일기 작성 알림 기능의 제공 여부는 추후 결정한다.

### 4.9 장소·지도 후속 구현

장소와 지도는 1차 MVP에서 제외한다. 1차 앱은 위치 권한을 요청하거나 미디어 GPS를 읽지 않으며 장소 데이터, 지도 화면, NAVER Maps SDK와 위치 기반 자동 날씨 API를 포함하지 않는다.

- 2차: 장소명 직접 입력, 선택적 GPS·현재 위치, 위치 카드와 좌표 기반 과거 날씨
- 3차: 네이버 지도 핀 선택, 저장 위치 보기, 추억 지도와 비용 통제

요구사항, 데이터 마이그레이션, 네이버 클라우드 월 0원 목표 설계와 실기기 검증 기준은 [장소·네이버 지도 도입 계획](readme/지도-도입-계획.md)에 보존한다.

## 5. 비기능 요구사항

- iOS와 Android를 모두 지원한다.
- 핵심 기능은 인터넷 연결 없이 사용할 수 있어야 한다.
- 사진 라이브러리, 카메라와 저장소에는 필요한 시점에 최소 권한만 요청해야 한다.
- 앱이 잠기거나 백그라운드로 전환될 때 민감한 일기 내용이 노출되지 않아야 한다.
- 공유는 사용자가 명시적으로 실행한 일기에 한해 일회성 이미지 파일을 생성하는 방식이어야 한다.
- 공유용 임시 이미지가 생성되는 경우 공유 완료 또는 취소 후 안전하게 정리해야 한다.
- 공유 이미지에는 원본 미디어의 GPS 등 민감한 메타데이터를 포함하지 않아야 한다.
- 공유 항목과 화면 표시 옵션은 안전한 기본값을 제공하고 사용자의 마지막 선택을 기기 내부에 저장해야 한다.
- 1차 MVP는 위치 권한, 미디어 GPS와 지도 네트워크를 사용하지 않아야 한다.
- 저장 실패나 저장 공간 부족으로 기록이 유실되지 않도록 사용자에게 상태와 복구 방법을 안내해야 한다.
- 지원 OS 버전, 접근성, 성능 목표와 개인정보 보호 정책은 추후 정의한다.
- 특정 언어나 프레임워크에 종속되지 않도록 요구사항과 구현 결정을 구분한다.

## 6. 화면 및 사용자 흐름

### 6.1 화면 목록

| 화면 | 구분 | 주요 역할 |
| --- | --- | --- |
| 진입/잠금 | 제안 추가 | 앱 시작, 초기 안내, 잠금 해제 및 사용자 인증 |
| SNS형 피드 | 필수 | 이미지 중심의 일기 목록 탐색 |
| 캘린더 | 필수 | 날짜별 일기 확인 및 선택 |
| 일기 상세 | 제안 추가 | 저장된 일기와 미디어 상세 보기 |
| 일기 등록 | 필수 | 미디어 선택 또는 촬영, 날짜와 일기 내용 입력 |
| 미디어 편집 | 제안 추가 | 이모지, 필터와 텍스트 적용 및 미리 보기 |
| 일기 수정 | 필수 | 기존 일기 내용 및 미디어 편집 |
| 공유 미리 보기 | 제안 추가 | 일기를 공유용 이미지로 확인하고 내보내기 |
| 휴지통 | 필수 | 삭제한 일기의 남은 기간 확인, 복원과 즉시 영구 삭제 |
| 데이터 관리 | 필수 | 암호화 백업 생성·검증·복원과 데이터 전체 삭제 |
| 설정 | 필수 | 보안, 저장 공간, 앱 정보와 기타 옵션 관리 |

피드 레이아웃, 잠금 시간과 날씨 표시처럼 여러 선택지가 있는 기능은 해당 화면과 설정 양쪽에서 변경할 수 있게 하고 마지막 선택을 기억한다. 장소·지도 화면은 1차 앱의 내비게이션과 빈 자리에도 노출하지 않는다.

### 6.2 주요 사용자 흐름

1. 앱 진입 및 필요한 경우 잠금 해제
2. SNS형 피드 또는 캘린더에서 기존 기록 탐색
3. 등록 버튼 선택
4. 사진·영상 촬영 또는 라이브러리에서 선택
5. 미디어 편집 및 일기 내용 입력
6. 결과 미리 보기 및 암호화 저장
7. 피드, 캘린더 또는 상세 화면에서 저장 결과 확인

공유 흐름은 `일기 선택 → 직전 공유 옵션 복원 → 포함 항목 선택 → 공유용 이미지 생성 → 미리 보기 → 기기 공유 기능 호출 또는 취소` 순서로 진행한다.

## 7. 기능별 상세 실행 계획

### 7.1 우선순위와 범위 원칙

| 구분 | 포함 기능 |
| --- | --- |
| 1차 MVP 필수 | 로컬 암호화 저장, 암호화 백업·복원, 사진·영상 선택, 기본 이미지 편집, 일기 등록·상세·수정·휴지통, 선택 가능한 SNS형 피드, 캘린더, 수동 날씨 기록, 선택형 공유 이미지, 유연한 앱 잠금과 기본 설정 |
| 1차 구현값 | 미디어 최대 10개, 영상 최대 3개·각 30초·총 60초, 전체 300MB의 균형 프로필. 실기기 측정 후 조정 |
| 2·3차 분리 | 장소 기록, 위치 기반 과거 날씨와 네이버 지도는 [별도 계획](readme/지도-도입-계획.md)에 따라 구현 |
| 후속 검토 | 영상 편집, 계정과 동기화, 고급 검색·필터, 알림, 수익화 |

각 기능은 정상 흐름뿐 아니라 권한 거부, 인터넷 끊김, 저장 공간 부족과 외부 API 장애 흐름까지 구현해야 완료된 것으로 본다. 특정 언어나 프레임워크에 종속된 구현 방식은 기술 스택을 정한 뒤 별도 기술 설계에 기록한다.

### 7.2 진입 및 앱 잠금

- 최초 실행에서는 앱의 로컬 저장 원칙과 사진 정보 사용 목적을 안내한다.
- 사진과 카메라 권한은 최초 실행에 한꺼번에 요청하지 않고 해당 기능을 사용한 시점에 요청한다. 1차 앱은 위치 권한을 요청하지 않는다.
- 사용자는 설정에서 앱 잠금을 켜거나 끌 수 있어야 한다.
- 앱 잠금이 켜져 있으면 인증이 완료되기 전에 피드, 썸네일과 일기 내용을 표시하지 않는다.
- 앱이 백그라운드로 전환되면 앱 전환 화면에 민감한 내용이 보이지 않도록 가림 화면을 표시한다.
- 잠금 인증은 생체 인증을 우선하고 지원하지 않거나 실패하면 운영체제의 기기 암호를 사용한다.
- 사용자는 앱을 벗어난 뒤 다시 잠그는 시간을 `즉시`, `30초`, `1분`, `5분`, `15분`, `30분` 중에서 선택할 수 있어야 하며 마지막 선택을 기억해야 한다.
- 재인증 유예시간과 관계없이 백그라운드 전환 화면의 민감한 내용은 즉시 가린다.

완료 조건은 잠금 상태에서 딥 링크나 알림 경로로 들어와도 일기가 노출되지 않고, 권한을 거부한 사용자도 사용 가능한 범위 안에서 앱에 진입할 수 있는 것이다.

### 7.3 SNS형 피드

- 최신 일기부터 이미지 중심으로 표시하고 사용자가 `세로 카드`, `2열 격자`, `3열 격자` 중에서 레이아웃을 선택할 수 있게 한다.
- 앱은 마지막으로 선택한 피드 레이아웃을 기억하고 다음 진입 시 적용한다.
- 여러 미디어가 있는 일기는 세로 카드에서 가로 슬라이드로 탐색하고 격자에서는 대표 미디어만 표시한다.
- 각 항목에는 대표 미디어, 작성 날짜와 선택적으로 한 줄·기분을 표시한다.
- 영상은 자동 재생하지 않고 대표 썸네일과 영상 표시 아이콘을 사용한다.
- 항목을 선택하면 일기 상세 화면으로 이동하고, 빈 상태에서는 첫 일기 작성 버튼을 제공한다.
- 데이터가 많아져도 한 번에 모든 원본 미디어를 복호화하지 않도록 페이지 단위로 읽고 암호화된 전용 썸네일을 사용한다.

완료 조건은 오프라인에서도 저장된 일기를 최신순으로 탐색할 수 있고, 스크롤 중 원본 미디어 전체를 메모리에 올리지 않는 것이다.

### 7.4 캘린더

- 월 단위 달력에 일기가 있는 날짜와 해당 날짜의 일기 개수를 표시한다.
- 날짜를 선택하면 그날의 일기 목록을 보여주고 상세 화면으로 이동할 수 있게 한다.
- 이전·다음 달 이동과 오늘로 돌아가기 기능을 제공한다.
- 일기 날짜를 수정하거나 삭제하면 캘린더 표시가 즉시 갱신되어야 한다.
- 시간대 변경 시 기존 일기의 날짜가 의도치 않게 바뀌지 않도록 저장 시각과 일기 기준 날짜를 구분한다.

완료 조건은 같은 날짜에 여러 일기가 있어도 모두 접근할 수 있고 피드와 캘린더의 결과가 일치하는 것이다.

### 7.5 일기 등록

1. 사용자가 카메라 또는 라이브러리에서 사진·영상을 한 개 이상 선택한다.
2. 선택한 미디어의 촬영 시각을 읽을 수 있으면 날짜 후보로만 제안하고 GPS 메타데이터는 읽지 않는다.
3. 사용자가 미디어를 편집하고 선택 입력 필드를 작성한다.
4. 미리 보기에서 저장될 결과를 확인한다.
5. 미디어 암호화와 일기 데이터 저장을 하나의 작업으로 처리한다.
6. 저장이 완료되면 피드와 캘린더에 결과를 반영한다.

- 작성 중 앱이 백그라운드로 전환되어도 입력 내용을 잃지 않도록 암호화된 임시 초안을 사용한다.
- 사용자가 취소하면 임시 미디어와 편집 결과를 제거한다.
- 중복 저장을 방지하기 위해 저장 버튼을 연속으로 눌러도 하나의 일기만 생성되도록 한다.
- 저장 공간 부족이나 암호화 실패 시 기존 데이터는 유지하고 재시도 또는 미디어 축소 방법을 안내한다.

완료 조건은 필수 미디어만으로 저장할 수 있고, 선택 필드가 비어 있어도 모든 탐색 화면에서 오류가 발생하지 않는 것이다.

### 7.6 미디어 선택 및 편집

- 라이브러리 선택과 앱 내 촬영을 모두 지원하며 필요한 시점에만 권한을 요청한다.
- 이미지 편집 MVP에는 이모지·스티커 배치, 필터, 텍스트 추가와 이동, 실행 취소·다시 실행을 포함한다.
- 편집은 가능한 한 원본을 훼손하지 않는 방식으로 처리하고, 원본과 편집 명령 또는 최종 렌더링 결과를 암호화하여 저장한다.
- 큰 이미지는 화면용 미리 보기로 편집하고 저장 또는 공유 시 목표 해상도로 렌더링해 메모리 사용량을 제한한다.
- 영상은 MVP에서 재생, 대표 프레임 선택과 정지 이미지 공유만 구현한다. 자르기·필터·텍스트 합성 등 영상 편집은 후속 버전으로 미룬다.

#### 미디어 제한 기술 검증 계획

최종 상한을 임의로 확정하지 않고 다음 세 프로필을 실제 iOS·Android 기기에서 비교한다. `균형` 프로필을 최초 구현값으로 사용하고 테스트 결과에 따라 한 단계 낮추거나 높인다.

| 프로필 | 전체 미디어 | 영상 개수 | 영상 1개 길이 | 영상 총길이 | 일기당 가져오기 용량 후보 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 절약 | 최대 5개 | 최대 1개 | 30초 | 30초 | 150MB |
| 균형(초기값) | 최대 10개 | 최대 3개 | 30초 | 60초 | 300MB |
| 확장 | 최대 20개 | 최대 5개 | 60초 | 180초 | 1GB |

사용자에게는 다음 저장 품질 옵션을 제공하고 마지막 선택을 기억한다.

- 사진: `원본 보관`, `저장 공간 절약`
- 영상: `720p 절약`, `1080p 균형`, `지원되는 경우 원본 보관`
- 저장 전 예상 추가 용량과 기기의 남은 공간을 표시한다.
- 원본 형식이 현재 기기에서 안정적으로 재생되지 않거나 안전 상한을 넘으면 호환 형식으로 최적화하도록 안내한다.

검증용 미디어 묶음에는 JPEG, PNG, HEIC·HEIF, WebP, 투명 이미지, 회전 메타데이터, HDR 사진과 MP4·MOV, H.264·HEVC, HDR, 가변 프레임률, 슬로모션, 4K·8K 및 손상 파일을 포함한다.

검증은 다음 순서로 수행한다.

1. 지원할 최소 사양 iPhone과 4GB RAM급 Android 실기기, 중간 사양 및 최신 기기를 선정한다.
2. 각 프로필의 미디어를 가져오고 썸네일·대표 프레임을 생성한 뒤 암호화 저장한다.
3. 저장한 일기를 재실행 후 열고 카드 슬라이드, 격자 피드, 수정과 공유를 반복한다.
4. 같은 데이터로 암호화 백업을 생성하고 새 설치 상태에서 복원한다.
5. 저장 중 앱 종료, 작업 취소, 저장 공간 부족, 손상 파일과 변환 실패를 재현한다.
6. 처리 시간, 최대 메모리, 결과 용량, 프레임 끊김, 배터리와 발열을 기록한다.

최종 제한은 다음 조건을 만족하는 가장 큰 프로필로 확정한다.

- 최소 사양 실기기에서 메모리 부족이나 앱 종료 없이 가져오기·암호화·열람·백업·복원이 완료된다.
- 오래 걸리는 작업은 백그라운드 처리하고 진행률과 취소를 제공하며 화면 입력을 막지 않는다.
- 실패 또는 취소 후 암호화되지 않은 임시 파일과 고아 파일이 남지 않는다.
- 저장 전에 예상 용량을 계산하고 공간 부족을 사전에 차단한다.
- 피드에서는 원본 대신 전용 썸네일을 사용하며 여러 원본을 동시에 복호화하지 않는다.

공식 구현 참고 문서:

- [Apple AVFoundation 미디어 변환](https://developer.apple.com/documentation/avfoundation/exporting-video-to-alternative-formats)
- [Android Media3 Transformer 지원 형식](https://developer.android.com/media/media3/transformer/supported-formats)

완료 조건은 편집 전후 결과를 미리 볼 수 있고, 저장 후 다시 열었을 때 같은 결과가 재현되며 원본 GPS 메타데이터가 공유 파일로 복사되지 않는 것이다.

### 7.7 수동 날씨 기록

- 1차 MVP의 날씨는 API나 위치 데이터 없이 사용자가 직접 입력하는 선택 필드다.
- 과거 일기에도 날씨 상태 아이콘, 대표 기온, 최저·최고기온과 강수 여부를 선택적으로 기록할 수 있다.
- 사용자는 각 항목을 표시하거나 숨길 수 있고 일기 상세와 공유 이미지의 마지막 표시 선택을 기억한다.
- 날씨를 입력하지 않아도 일기를 저장할 수 있으며 오늘·미래 날짜를 기술적으로 차단하지 않는다.
- 입력한 날씨는 다른 일기 데이터와 함께 암호화하고 외부 서비스로 전송하지 않는다.
- 좌표 기반 기상청 과거 날씨 제안은 장소 기록과 함께 2차 구현으로 이동한다.

완료 조건은 네트워크 연결과 위치 권한 없이 날씨를 입력·수정·삭제하고 오프라인 일기에 저장할 수 있는 것이다. 자동 과거 날씨의 기술 검증과 공급자 후보는 [장소·네이버 지도 도입 계획](readme/지도-도입-계획.md)에 보존한다.

### 7.8 일기 상세, 수정 및 삭제

- 상세 화면은 미디어, 날짜·시간, 한 줄, 본문, 기분, 태그, 날씨와 함께한 사람 중 저장된 항목만 표시한다.
- 여러 미디어가 있으면 순서대로 탐색할 수 있고 영상은 사용자가 눌렀을 때만 재생한다.
- 수정은 등록 화면과 같은 검증·미리 보기·암호화 저장 절차를 재사용한다.
- 삭제 전 확인한 뒤 일기와 관련 미디어를 암호화된 휴지통으로 이동한다.
- 휴지통에서는 삭제 예정일을 표시하고 30일 안에 복원하거나 즉시 영구 삭제할 수 있게 한다.
- 30일이 지난 일기는 다음 앱 실행·복귀 시 데이터 레코드·원본 미디어·편집본·썸네일과 관련 키를 반드시 영구 삭제하고, 운영체제가 허용할 때는 예약 백그라운드 작업으로 먼저 정리한다.
- 영구 삭제 중 실패하면 일부 파일만 남거나 레코드만 사라지지 않도록 복구 가능한 상태를 유지한다.

완료 조건은 수정·복원 결과가 피드와 캘린더에 즉시 반영되고, 영구 삭제 시 일기별 암호화 키를 제거하는 암호학적 삭제를 수행해 남은 저장 블록을 복호화할 수 없는 것이다. 플래시 저장장치의 물리 바이트 덮어쓰기는 보장한다고 표현하지 않는다.

### 7.9 공유 이미지

1. 사용자가 일기 상세 화면에서 공유를 명시적으로 실행한다.
2. 직전에 사용한 공유 선택 상태를 불러온다.
3. 공유에 포함할 한 줄·본문·날짜·기분·날씨를 사용자가 변경한다.
4. `4:5`, `1:1`, `9:16` 비율과 기본·밝은·어두운 테마 중 하나를 선택하고 워터마크 표시 여부를 정한다.
5. 정확한 좌표와 원본 EXIF를 제외한 정지 이미지를 임시 영역에 생성한다.
6. 미리 보기 후 운영체제의 공유 화면을 호출한다.
7. 공유 완료, 취소 또는 제한 시간이 지나면 임시 파일을 삭제한다.

- 영상 일기는 선택한 대표 프레임을 사용하고 영상 파일 자체는 내보내지 않는다.
- 공유 항목, 비율, 테마와 워터마크 선택은 다음 공유의 기본값으로 기억한다.
- 생성 도중 앱이 종료된 경우 다음 실행 시 남은 임시 공유 파일을 정리한다.

완료 조건은 앱 외부로 전달되는 파일에 좌표·원본 위치 메타데이터가 없고, 사용자가 공유를 취소하면 외부 전송이 발생하지 않는 것이다.

### 7.10 설정, 저장 공간 및 개인정보

- 설정 화면에는 앱 잠금, 개인정보 안내, 저장 공간 사용량, 앱 버전과 오픈소스 고지를 제공한다.
- 앱 잠금 사용 여부, 재인증 시간, 피드 레이아웃과 날씨 표시 옵션을 설정에서 변경할 수 있게 한다.
- 미디어 원본·편집본·썸네일이 차지하는 공간을 구분해 표시한다.
- 암호화 백업 생성, 백업 파일 검증과 복원 기능을 MVP에 제공한다.
- 백업에는 일기 데이터, 수동 날씨, 원본 미디어, 편집 정보와 필요한 설정을 포함하고 백업 포맷 버전과 무결성 정보를 기록한다.
- 백업 파일은 사용자가 정한 암호 또는 복구 키 없이는 열 수 없어야 하며 평문 내보내기를 기본 경로로 제공하지 않는다.
- 복원 전 백업 생성 시각, 일기 수와 미디어 수를 확인하고 기존 데이터에 병합할지 교체할지 선택할 수 있게 한다.
- 손상된 백업, 잘못된 암호와 지원하지 않는 백업 버전은 기존 데이터를 변경하지 않고 오류를 안내해야 한다.
- 데이터 전체 삭제는 재확인과 사용자 인증 후 실행하고 삭제 범위와 되돌릴 수 없음을 명확히 안내한다.

완료 조건은 사용자가 저장 공간과 개인정보 처리 범위를 확인할 수 있고, 데이터 전체 삭제 후 복호화 가능한 일기·미디어와 암호화 키가 남지 않는 것이다.

### 7.11 계정, 동기화, 검색 및 알림

- MVP는 로그인 없이 로컬 단일 기기 사용을 기본으로 한다.
- 계정과 동기화를 도입할 때는 종단 간 암호화, 키 복구, 충돌 해결과 탈퇴 시 데이터 삭제 정책을 먼저 설계한다.
- 태그·날짜·기분 검색은 로컬 데이터만 대상으로 시작하고 본문 검색 범위는 성능과 개인정보 영향을 확인한 뒤 확장한다.
- 일기 작성 알림은 사용자가 시간과 반복 주기를 직접 설정하고 언제든 끌 수 있는 선택 기능으로 검토한다.

## 8. 데이터 및 시스템 설계 계획

### 8.1 논리 데이터 구조

| 데이터 | 주요 항목 | 저장 원칙 |
| --- | --- | --- |
| Diary | 식별자, 일기 기준 날짜·시간, 한 줄, 본문, 기분, 태그, 날씨, 함께한 사람, 생성·수정 시각, 휴지통 이동·영구 삭제 예정 시각 | 기기 내부 암호화 데이터베이스 |
| Media | 식별자, 일기 식별자, 종류, 순서, 암호화 파일 경로, 썸네일 경로, 촬영 시각 후보, 편집 정보 | 암호화 파일과 암호화 메타데이터를 분리 관리 |
| WeatherEntry | 날씨 상태, 대표·최저·최고기온, 강수 여부, 사용자가 표시할 항목 | 사용자가 입력한 값만 Diary와 함께 암호화 |
| Draft | 작성 중 필드, 임시 미디어 참조, 마지막 수정 시각 | 암호화된 임시 영역에 저장하고 완료·취소 후 정리 |
| AppSettings | 잠금 사용 여부와 재인증 시간, 피드 레이아웃, 미디어 저장 품질, 날씨 표시, 직전 공유 항목, 개인정보 동의 상태 | 민감도에 따라 암호화 저장 또는 운영체제 보안 저장소 사용 |
| BackupManifest | 백업 포맷 버전, 생성 시각, 데이터·미디어 목록, 무결성 정보 | 암호화된 백업 패키지 안에 저장 |

미디어 촬영 시각은 자동 확정 값이 아니라 날짜 후보 값으로 구분한다. 1차 데이터 구조에는 장소와 좌표를 저장하지 않으며 2차에서 버전 마이그레이션으로 추가한다.

### 8.2 암호화와 키 관리

- 일기 데이터베이스와 미디어 파일은 저장 시 암호화한다.
- 데이터 암호화 키는 데이터 파일과 분리하고 iOS Keychain 및 Android Keystore 계열의 운영체제 보안 저장소로 보호한다.
- 썸네일, 임시 초안과 공유 전 렌더링 파일도 민감 데이터로 취급한다.
- 암호화 키 생성, 회전, 앱 재설치와 기기 변경 시 동작은 구현 전에 위협 모델과 함께 정의한다.
- 오류 로그, 분석 이벤트와 크래시 보고서에 일기 본문, 파일 경로와 검색어를 기록하지 않는다.

### 8.3 저장 트랜잭션과 파일 수명 주기

1. 선택한 미디어를 앱 전용 임시 영역으로 가져온다.
2. 편집 결과와 썸네일을 생성하고 각각 암호화한다.
3. 모든 파일 쓰기가 성공하면 일기 데이터 레코드를 확정한다.
4. 실패하면 새로 만든 파일만 정리하고 기존 일기는 유지한다.
5. 앱 시작 시 고아 임시 파일과 만료된 공유 파일을 안전하게 정리한다.

### 8.4 시스템 경계

```text
모바일 앱
├─ 암호화 데이터베이스: 일기·수동 날씨·설정 메타데이터
├─ 암호화 파일 저장소: 원본·편집본·썸네일·초안
└─ 암호화 백업·복원: 사용자 선택 파일로 내보내기·가져오기
```

1차 MVP에는 중앙 서버, 지도 SDK와 날씨 서버리스 함수를 두지 않는다. 장소·지도·자동 과거 날씨를 추가할 때의 시스템 경계는 별도 후속 계획에서 정의한다.

### 8.5 테스트 계획

| 영역 | 필수 검증 |
| --- | --- |
| 데이터 | 필수값 검증, 같은 날짜의 여러 일기, 수정·휴지통·복원·영구 삭제와 마이그레이션 |
| 보안 | 저장 파일 평문 노출 여부, 키 분리, 잠금 우회, 로그·공유 파일의 위치 정보 제거 |
| 미디어 | 대용량 사진, 긴 영상, 손상 파일, 메모리 부족, 편집 결과 재현 |
| 범위 경계 | 위치 권한·GPS·지도 SDK·장소 데이터·날씨 네트워크 호출이 1차 빌드에 없는지 확인 |
| 날씨 | 수동 입력·수정·삭제, 빈 값, 기온 범위와 표시 옵션 |
| 백업 | 올바른 암호, 잘못된 암호, 중단·손상 파일, 버전 차이, 복원 후 무결성 |
| 오프라인 | 등록·수정·삭제·피드·캘린더 사용, 외부 API 실패 시 직접 입력 대체 흐름 |
| 플랫폼 | 지원 대상 iOS·Android 실제 기기, 사진 선택기와 카메라 권한, 백그라운드 복귀 |
| 공유 | 영상 대표 프레임, 선택 필드 반영, EXIF·좌표 제거, 취소 후 임시 파일 삭제 |

## 9. 기술 결정

### 9.1 앱 개발 방식

앱은 `Flutter + Dart` 단일 코드베이스로 iOS와 Android를 함께 개발하는 방식을 잠정 확정한다. 화면, 상태, 일기 도메인, 암호화 파일 포맷과 테스트는 공통으로 구현하고, 운영체제 보안·미디어·파일 선택과 개인정보 보호에 필요한 부분만 얇은 Kotlin·Swift 브리지로 분리한다. iOS와 Android 앱 전체를 서로 다른 언어로 중복 개발하지 않는다.

- 기준 Flutter 버전: 검증 시점의 stable `3.44.x`
- 최소 지원 후보: Android API 24, iOS 15
- 화면 이동: 공식 유지보수 범위의 `go_router`
- 로컬 데이터베이스: Drift + SQLite 계열. SQLCipher를 연결해 데이터베이스 파일·저널까지 암호화되는지 검증한 뒤 확정
- 미디어 선택·재생: Flutter 공식 `image_picker`, `video_player`를 우선 사용
- 지도 SDK, 위치 패키지와 날씨 서버리스 함수: 1차 MVP에서 제외
- 중앙 계정·일기 데이터 서버·동기화·분석 SDK: 1차 MVP에서 제외

### 9.2 네이티브 경계

| 경계 | 공통 Flutter 영역 | 네이티브 확인 또는 브리지 영역 |
| --- | --- | --- |
| 보안 | 암호화 포맷, 키 수명 주기와 잠금 정책 | Android Keystore, iOS Keychain, 앱 전환 화면 가림 |
| 미디어 | 선택 결과 관리, 편집 UI와 일기 연결 | Android Media3, iOS AVFoundation 기반 영상 정규화·썸네일·메타데이터 |
| 백업 | 백업 manifest, 암호화·무결성·복원 트랜잭션 | Android 문서 저장 선택기, iOS Document Picker |

`flutter_secure_storage` 같은 커뮤니티 패키지는 구현을 빠르게 시작하는 어댑터로만 취급한다. 보안 키의 기기 이전 금지, 앱 재설치와 기기 암호 변경 검증을 통과해야 운영 의존성으로 승인한다.

### 9.3 최종 확정 전 P0 검증 관문

Flutter 채택을 되돌리는 검증이 아니라, 패키지를 그대로 사용할지 자체 네이티브 브리지를 사용할지 결정하는 관문으로 운영한다.

1. 균형 프로필인 미디어 10개, 영상 최대 3개·각 30초·총 60초·총 300MB로 가져오기, 정규화, 썸네일, 청크 암호화, 열람, 편집 이미지 내보내기와 공유를 측정한다.
2. 같은 데이터로 암호화 백업 생성·중단·손상·잘못된 암호·병합·교체 복원을 검증하고 평문 임시 파일과 메모리 부족 여부를 검사한다.
3. 앱 잠금 켜기·끄기, 즉시·1분·5분·15분·30분 재인증, 재설치, 기기 암호 변경과 생체 인증 변경 시 키 동작을 검증한다.
4. 앱 권한·의존성·네트워크 검사를 실행해 1차 빌드에 위치 권한, 지도 SDK, NAVER Cloud 인증 정보와 날씨 API 호출이 없음을 확인한다.

최소 실기기가 없는 현재 환경에서는 문서·패키지·자동 판정 로직까지만 검증 완료로 표시하고, 위 항목은 결과 보고서가 생성되기 전까지 최종 통과로 간주하지 않는다. 장소·지도 검증 관문은 2·3차 계획 문서에서 별도로 관리한다.

## 10. 개발 단계

### 10.1 권장 실행 순서

1. 지원할 최소 iOS·Android 버전과 미디어 검증용 실기기를 정한다.
2. **미디어 제한 기술 검증을 가장 먼저 수행**하고 균형 프로필로 가져오기·암호화·열람·백업·복원을 측정한다.
3. 결과에 따라 미디어 개수, 영상 길이, 저장 품질과 용량 상한을 확정한다.
4. 확정한 미디어 규격으로 로컬 암호화 저장소, 초안과 백업 포맷을 고정한다.
5. 핵심 일기·미디어·피드·캘린더·휴지통·잠금 기능을 구현한다.
6. 수동 날씨, 선택형 공유 이미지와 암호화 백업·복원을 완성한다.
7. 권한·의존성·네트워크 검사를 통해 장소·지도·자동 날씨 기능이 1차 빌드에 포함되지 않았는지 확인한다.

미디어 검증은 데이터 구조와 저장 비용을 결정하므로 다른 기능보다 먼저 완료한다. 장소·지도·자동 과거 날씨 조사 결과와 기존 검증 도구는 보존하되 1차 앱에는 연결하지 않는다.

### 10.2 단계별 계획

| 단계 | 주요 작업 | 완료 기준 |
| --- | --- | --- |
| 1. 요구사항 확정 | 미결정 사항 중 MVP 차단 항목 결정, 화면 흐름과 데이터 항목 확정 | 화면별 범위와 수용 기준 승인 |
| 2. 미디어 기술 검증 | 균형 프로필을 기준으로 미디어 가져오기·변환·암호화·피드·백업 성능 비교 | 안전하게 지원할 미디어 제한과 저장 품질 확정 |
| 3. 기반 기술 검증 | Flutter 양 플랫폼 최소 앱, 암호화 키 관리와 백업 파일 선택 검증 | 실제 iOS·Android 기기에서 1차 핵심 기술 검증 성공 |
| 4. 기반 구축 | 프로젝트 구조, 로컬 DB, 암호화 파일 저장소, 키 관리, 오류 처리와 테스트 기반 구성 | 샘플 일기를 안전하게 저장·복호화·삭제 가능 |
| 5. 핵심 일기 기능 | 등록, 선택형 피드, 캘린더, 상세, 수정, 휴지통과 복원 구현 | 오프라인 CRUD와 30일 삭제 사용자 흐름 통과 |
| 6. 미디어 기능 | 사진·영상 선택, 이미지 편집, 썸네일, 미리 보기와 임시 초안 구현 | 확정한 범위의 미디어로 저장·재편집 성공 |
| 7. 공유·설정 | 선택형 공유 이미지, 수동 날씨, 임시 파일 정리, 잠금 옵션, 암호화 백업·복원과 개인정보·저장 공간 설정 구현 | 민감 정보 제거, 잠금과 복원 검증 통과 |
| 8. 안정화 | 성능·접근성·보안·마이그레이션·실기기 테스트와 범위 경계 검사 | 출시 차단 결함 없고 위치·지도 의존성 없음 |
| 9. 배포 준비 | 개인정보 처리방침, 스토어 정보, 권한 문구, 서명·빌드 자동화 | App Store·Google Play 심사 제출 가능 상태 |

## 11. 미결정 사항

- 현재 임시 제한(미디어 10개, 영상 3개·각 30초·총 60초, 총 300MB)을 실기기 측정 후 그대로 확정할지
- 영상 1080p 정규화를 1차 안정화에 넣을지, 원본 보관과 제한 안내로 유지할지
- 공유 이미지 비율별 출력 해상도와 렌더링 품질
- 로그인 및 계정 필요 여부
- 서버와 데이터 동기화 필요 여부
- 앱 스토어 배포 전 복구 키 보관 안내와 기기 변경 실제 사용자 흐름
- 태그·날짜·기분 등을 이용한 검색 및 필터 기능의 제공 범위
- 일기 작성 알림 기능의 필요 여부
- 수익화 방식
- 출시 목표와 지원 국가 및 언어

장소, 자동 과거 날씨와 네이버 지도 관련 미결정 사항은 [장소·네이버 지도 도입 계획](readme/지도-도입-계획.md)에서 2·3차 범위로 관리한다.

## 12. 변경 이력

| 날짜 | 변경 내용 |
| --- | --- |
| 2026-08-30 | 최초 문서 생성. iOS·Android 지원 및 기술 스택 추후 결정 원칙 기록 |
| 2026-08-30 | 그림일기장 목적, 미디어 등록·편집, 캘린더·SNS형 피드, 암호화 저장, 화면 구성 요구사항 추가 |
| 2026-08-30 | 앱 내부 공개 기능 제외 및 사용자 주도의 이미지 공유 기능 확정 |
| 2026-08-30 | 일기 입력 필드 후보를 MVP와 추후 선택 항목으로 구분하여 제안 |
| 2026-08-30 | 사진·영상만 필수로 확정하고 나머지 입력 필드를 선택 사항으로 변경. 미디어 위치 기반 장소 제안과 날씨 API 연동 원칙 추가 |
| 2026-08-30 | 위치 지도 표시, 장소·주소 검색, 지도 핀 지정 요구사항과 네이버 지도 API 후보 추가 |
| 2026-08-30 | 지도 서비스 후보 비교 및 국내 우선 MVP에 카카오 지도·로컬 API를 잠정 제안했으나 이후 네이버 확정으로 대체 |
| 2026-08-30 | 지도 후보별 공식 무료 한도·초과 단가·서버 필요성 비교 및 무상태 서버리스 중계 구조 제안 |
| 2026-08-30 | 지도 서비스를 네이버로 확정하고 기능·화면별 실행 계획, 데이터·보안 구조, 테스트와 단계별 개발 계획 추가 |
| 2026-08-30 | 지도를 위치 기록·추억 용도로 축소하고 장소·주소 검색을 MVP에서 제외. 현재 위치 버튼과 과거 날씨 스냅샷 확정 |
| 2026-08-30 | 앱 잠금 사용 여부·재인증 시간을 선택 가능하게 하고 암호화 백업·복원, 30일 휴지통, 선택형 피드·공유 옵션을 MVP에 추가 |
| 2026-08-30 | 미디어 제한 후보 프로필·실기기 검증 기준과 저장 품질 옵션을 추가하고 미디어 검증을 최우선 기술 작업으로 배치 |
| 2026-08-30 | 기상청 ASOS를 과거 날씨 1차 후보로 정하고 관측소 기반 개인정보 최소화 구조, 30개 표본 검증과 WeatherKit 대체 조건 추가 |
| 2026-08-30 | 플랫폼 중립 미디어 판정 하네스와 과거 날씨 관측소 매핑·정규화·30개 표본 검증 도구 구현. 실기기와 기상청 인증키가 필요한 라이브 검증은 대기 |
| 2026-08-30 | Flutter 단일 코드베이스와 얇은 Kotlin·Swift 브리지 구조를 잠정 확정하고, 지도·미디어·보안·백업·날씨의 P0 실기기 검증 관문 추가 |
| 2026-08-30 | 네이버 지도를 사용자 요청 시에만 생성하고 단일 지도 인스턴스·콘솔 하드 한도·무료량 안전 상한을 적용하는 월 0원 목표 비용 설계 추가 |
| 2026-08-30 | 장소·지도·자동 위치 기반 날씨를 1차 MVP에서 제거. 2차 장소 기록과 3차 네이버 지도 도입 계획을 `readme/지도-도입-계획.md`로 분리 |
